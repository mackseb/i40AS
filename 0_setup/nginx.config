server {
    listen 80;
    server_name 138.68.105.170;

    location / { try_files $uri @i40AS; }
    location @i40AS {
        include uwsgi_params;
        uwsgi_pass unix:/tmp/nginx_client_flaskzmq.sock;
    }
}
